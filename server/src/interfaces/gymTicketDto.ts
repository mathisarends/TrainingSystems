/**
 * Data Transfer Object (DTO) used for uploading or retrieving gym tickets.
 * This DTO encapsulates the gym ticket information as a base64-encoded string.
 */
export interface GymTicketDto {
  gymTicket: string;
}
