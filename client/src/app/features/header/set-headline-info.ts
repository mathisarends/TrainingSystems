/**
 * Interface to ensure a component implements the `setHeadlineInfo` method.
 */
export interface SetHeadlineInfo {
  /**
   * Sets the headline information for the component.
   */
  setHeadlineInfo(data?: any): void;
}
