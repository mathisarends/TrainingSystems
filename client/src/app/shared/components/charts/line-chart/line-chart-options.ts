export interface LineChartOptions {
  yAxisTitle?: string; // Title for the Y-axis
  maintainAspectRatio?: boolean; // Whether to maintain the aspect ratio
  responsive?: boolean; // Whether the chart should be responsive
}
